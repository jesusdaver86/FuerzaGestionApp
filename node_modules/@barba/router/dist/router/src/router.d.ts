/**
 * @barba/router
 * <br><br>
 * ## Barba Router.
 *
 * - Add `route` to Barba transitions resolution
 *
 * @module router
 * @preferred
 */
/***/
import { IBarbaPlugin, ITransitionData } from '@barba/core/src/defs';
import { Core } from '@barba/core/src/core';
import { Logger } from '@barba/core/src/modules/Logger';
import { IRouteByName, IRouteResolved, IRouterOptions } from './defs';
declare class Router implements IBarbaPlugin<IRouterOptions> {
    name: string;
    version: string;
    barba: Core;
    logger: Logger;
    routeNames: string[];
    routesByName: IRouteByName;
    /**
     * Plugin installation.
     */
    install(barba: Core, { routes }?: IRouterOptions): void;
    /**
     * Plugin initialisation.
     */
    init(): void;
    /**
     * Resolve URL to route name.
     */
    resolveUrl(url: string): IRouteResolved | null;
    /**
     * Hooks: do, reset.
     *
     * - Update `current` and `next` data
     */
    resolveRoutes(data: ITransitionData): void;
}
declare const router: Router;
export default router;
